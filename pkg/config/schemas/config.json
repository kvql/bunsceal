{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/kvql/bunsceal/pkg/config/schemas/config.json",
  "title": "Taxonomy Configuration",
  "description": "Configuration for taxonomy terminology and customization",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "terminology": {"$ref": "./terms.json#/$defs/terms"},
    "schema_path": {
      "$ref": "./common.json#/$defs/filePath",
      "description": "Directory path containing JSON schema files for validation. Defaults to './schema' if not specified."
    },
    "fs_repository": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "taxonomy_path": { "$ref": "./common.json#/$defs/filePath",
          "description": "Directory path containing JSON schema files for validation. Defaults to './schema' if not specified."
        },
        "l1_dir": {
          "$ref": "./common.json#/$defs/filePath",
          "description": "Name of directory with L1 taxonomy files"
        },
        "l2_dir": {
          "$ref": "./common.json#/$defs/filePath",
          "description": "Name of directory with L2 taxonomy files"
        }
      }
    },
    "visuals": { "$ref": "./visualise.json#/$defs/visuals"},
    "rules": {
      "type": "object",
      "description": "Configuration for business logic validation rules",
      "additionalProperties": false,
      "properties": {
        "shared_service": {
          "type": "object",
          "description": "Configuration for shared-service environment validation rule",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Enable or disable the shared-service validation rule",
              "default": true
            }
          }
        },
        "uniqueness": {
          "type": "object",
          "description": "Configuration for uniqueness validation across taxonomy entities",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Enable or disable uniqueness validation",
              "default": true
            },
            "check_keys": {
              "type": "array",
              "description": "List of field keys to check for uniqueness across entities",
              "items": {
                "type": "string"
              },
              "default": ["name"]
            }
          }
        }
      }
    },
    "plugins": {
      "$ref": "./plugins.json"
    }
  }
}
